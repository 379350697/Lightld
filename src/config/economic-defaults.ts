// Conservative full-lifecycle estimate for a personal DLMM position. It
// covers tipped entry/exit swaps plus the base fees of multiple LP legs.
export const DEFAULT_ROUND_TRIP_CHAIN_COST_SOL = 0.0001;
